numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3        # only needed if you use the MICE fallback
statsmodels>=0.14
matplotlib>=3.7
torch>=2.0
# Optional but strongly recommended — exact match to the plan's MICE arm:
#   R must be installed separately, with:
#       R -e 'install.packages("mice")'
rpy2>=3.5                # optional
